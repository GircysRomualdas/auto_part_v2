<h1>Show customer</h1>
<div>
  <p><%= @customer.id %></p>
  <p><%= @customer.email %></p>
  <p><%= @customer.created_at %></p>
  <p><%= @customer.updated_at %></p>
</div>
<%= button_to "Delete", admin_customer_path(@customer), class: "btn btn-danger", method: :delete, data: {turbo_confirm: "Are you sure?"} %>
<%= link_to "back", admin_customers_path %>